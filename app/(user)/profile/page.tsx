/**
 * Placholder for the profile landing page.
 *
 * @see middleware.ts for the logic that handles redirection to the user's profile page.
 */
export default async function ProfileLandingPage() {
  return null;
}
